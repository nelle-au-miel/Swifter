//
//  ContentView.swift
//  Exercices
//
//  Created by Jaynelle Latortue-Raymond (Étudiant) on 2026-08-28.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
                        
            Button("Clique") {
                
                // COURS #1
                
                // exe 1
                print("Bonjour Swift")
                
                // exe 2
                var videoGame = "Minecraft"
                print(videoGame)
                
                videoGame = "Stardew Valley"
                
                let myName = "Jaynelle"
                
                // exe 4
                print("I'm \(myName) and I like \(videoGame)")
                
                // exe 5
                let name = "Swift"
                let version = "6.0.61f"
                let nbUsers = 250
                var prix = 149.00
                let developer = "Apple Inc."
                
                print("\(name) regroups \(nbUsers) users and was made by \(developer), It's lastest version is \(version), which costs \(prix)$.")
                
                let rabais = 28.50
                prix = prix - rabais
                
                print("But there's a promotion! and it's \(prix)$ for a limited time.")
                
                
                print("---------------------------------------------------------------------")
                
                
                // COURS #1
                
                // exe 1
                let client = "Peggy"
                var prixUnit = 25.00
                var qte = 4.0
                var membre = true
                let code = "SWIFT10"
                
                var sousTotal = prixUnit * qte
                print(sousTotal)
                
                
                // exe 2
                if qte > 0 && qte <= 4 {
                    sousTotal = sousTotal
                }
                else if qte >= 5 && qte <= 9 {
                    sousTotal = sousTotal * 0.05
                }
                else if qte >= 10{
                    sousTotal = prixUnit * 0.10
                }
                print(sousTotal)
                
                
                // exe 3
                if membre {
                    sousTotal = sousTotal * 0.05
                }
                
                
                // exe 4
                if code == "SWIFT10" {
                    sousTotal = sousTotal * 0.10
                }
                
                
                // exe 5
                let total = sousTotal
                
                
                // exe 6
                if total < 50.00 {
                    print("Petite commande")
                }
                else if total >= 50.00 && total <= 99.99 {
                    print("Commande moyenne")
                }
                else {
                    print("Grande commande")
                }
                
                
                // exe 7
                if qte <= 0 {
                    print("Erreur : quantité invalide -- aucun calcul de commande ne doit être effectué.")
                }
                
                // Affichage
                print("------ COMMANDE ------")
                print("")
                print("")
                print("----------------------")
            }
        }
        .padding()
    }
}
